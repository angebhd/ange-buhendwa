import type {Config} from "tailwindcss"

const config:Config = {
    theme :{
        extend:{
            colors:{
                black:{
                    DEFAULT: '#000',
                    100: '#000319'
                }
            }
        }
    }
}